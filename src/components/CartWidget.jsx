const CartWidget = ({ counter }) => {
  // console.log(props)
  // const {counter}= props
  return (
    <div>
      <span>🛒</span>
      <span>{counter}</span>
    </div>
  );
};
export default CartWidget;
